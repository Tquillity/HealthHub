# Dependencies
node_modules/
/node_modules
.node_modules
/.pnp
.pnp.js
.pnp.loader.mjs

# Testing
/coverage

# Next.js – core build output (covers both Webpack and Turbopack)
/.next/
.next
.next/
/out/
/build/

# Turbopack-specific dev-time cache and artifacts (Next.js 15–16+)
/.next/dev/
/.next/cache/
/.next/turbopack/
/.next/*.db
.next/*.log
.next/trace
.next/lock
.next/turbopack.lock

# Turborepo cache and logs
.turbo/
**/.turbo/
*.turbo.log*

# Production builds (some frameworks/Next export)
/dist/

# Misc OS / editor files
.DS_Store
*.pem
Thumbs.db
desktop.ini

# Debug logs
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
ts-debug.log*

# Local environment variables
.env
.env*.local
.env*.development
.env*.production

# Vercel / Netlify / Cloudflare
.vercel
.netlify
.cloudflare

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Package manager lock files (only commit the one you actually use)
# For pnpm projects – commit pnpm-lock.yaml, ignore the others
# 
# IMPORTANT: Currently package-lock.json is tracked because pnpm-lock.yaml doesn't exist yet.
# Once pnpm-lock.yaml is generated (via `pnpm install`), uncomment the line below
# and remove package-lock.json from git tracking.
#
# package-lock.json    # ← uncomment once pnpm-lock.yaml exists
# yarn.lock            # ← uncomment if you ever switch to yarn
!pnpm-lock.yaml        # ← explicitly allow pnpm-lock.yaml to be committed

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Optional: Prisma binary artifacts (if you use Prisma)
prisma/dev.db
prisma/*.db

# Optional: Any local SQLite databases
*.db
*.sqlite
*.sqlite3

# MCP
mcp.json